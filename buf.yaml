version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Türkçe yorumlar kullanıyoruz; buf'ın yorum zorunluluğu kuralları
    # İngilizce doküman üretimi varsayar ve burada değer katmıyor.
    - COMMENT_FIELD
    - COMMENT_MESSAGE
    - COMMENT_ENUM
    - COMMENT_ENUM_VALUE
breaking:
  use:
    - FILE
